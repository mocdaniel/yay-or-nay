apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "yay-or-nay.fullname" . }}-test-connection"
  labels:
    {{- include "yay-or-nay.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "yay-or-nay.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
