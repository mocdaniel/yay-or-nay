apiVersion: v1
kind: Service
metadata:
  name: {{ include "yay-or-nay.fullname" . }}
  labels:
    {{- include "yay-or-nay.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "yay-or-nay.selectorLabels" . | nindent 4 }}
